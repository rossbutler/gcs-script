version: '3.3'
services:
  cloudflared-tunnel:
    image: cloudflare/cloudflared:2022.7.1
    command: tunnel --no-autoupdate run --token ${CF_TUNNEL_TOKEN}
    restart: always
    network_mode: host
  vless:
    image: rossbutler/vless:main
    network_mode: host
